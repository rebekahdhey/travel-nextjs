/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      // Next.js automatically optimizes images in the 'public' directory
      // You don't need to add anything here for local images
      unoptimized: false, // Optional: set to true if you want to disable optimization
    },
  };
  
  export default nextConfig;
  